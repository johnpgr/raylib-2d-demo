{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile",
            "type": "shell",
            "command": "mkdir -p bin && gcc -g -Wall -Wextra -std=c2x -lm ${relativeFile} -o bin/${fileBasenameNoExtension}"
        }
    ]
}
